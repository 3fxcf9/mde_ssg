## Pages

### Tiled (`template = tiled`)

Page:
- `title`
- `subtitle` (optional)

Card:
- `title`
- `subtitle` (optional)
- `date` (optional) Format: `DD/MM/YYYY`
- optional `cover.svg` inside the folder.

### List (`template = list`)

Page:
- `title`

Card:
- `title`
- `date` (optional) Format: `DD/MM/YYYY`
- `description` (optional)
- `keywords` (optional)
- optional `cover.svg` inside the folder.
